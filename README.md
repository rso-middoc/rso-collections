# RSO-MIDDOC: Document metadata microservice

## Prerequisites

```bash
docker run -d --name pg-collections -e POSTGRES_USER=dbuser -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=collections -p 5432:5432 postgres:12
```
